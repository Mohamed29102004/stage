{% extends 'base.html.twig' %}

{% block title %}Hello DashboardController!{% endblock %}

{% block main %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper login-form">
    <div class="example-wrapper Dash">
        <H1>Bienvenue sur le dashboard<H2>
            <a href="{{path('app_page_index')}}" class="btn btn-primary btn-lg" id="bmenu">Page</a>
            <a href="{{path('app_article_index')}}" class="btn btn-primary btn-lg" id="bmenu">Article</a>
            <a href="{{path('app_image_index')}}" class="btn btn-primary btn-lg" id="bmenu">Image</a>
    </div>
{% endblock %}
