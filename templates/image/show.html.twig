{% extends 'base.html.twig' %}product

{% block title %}Image{% endblock %}

{% block main %}
    <h1>Image</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>Chemin</th>
                <td>{{ image.Chemin }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ image.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_image_index') }}">back to list</a>

    <a href="{{ path('app_image_edit', {'id': image.id}) }}">edit</a>

    {{ include('image/_delete_form.html.twig') }}
{% endblock %}
