{% extends 'base.html.twig' %}

{% block title %}Page index{% endblock %}

{% block main %}

<div class="container-fluid">
    <div class="row">
        <div class="col12">
            <h1>Page index</h1>
        </div>
    </div>
    <div class="row">
        <div class="col12">
            <table class="table table-dark table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Auteur</th>
                <th>Nom</th>
                <th>PublicationAt</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for page in pages %}
            <tr class="align-middle">
                <td>{{ page.id }}</td>
                <td>{{ page.Auteur }}</td>
                <td>{{ page.Nom }}</td>
                <td>{{ page.PublicationAt ? page.PublicationAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ page.Description }}</td>
                <td>
                    <a href="{{ path('app_page_show', {'id': page.id}) }}" class="btn btn-primary btn-lg">Montrer</a>
                    <a href="{{ path('app_page_edit', {'id': page.id}) }}" class="btn btn-primary btn-lg">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">Aucune page n'a était trouver</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
            <a href="{{ path('app_page_new') }}" class="btn btn-primary btn-lg">Ajouter un page</a>
        </div>
    </div>
</div>
    
{% endblock %}
