{% extends 'base.html.twig' %}

{% block title %}Page{% endblock %}

{% block main %}

<div class="container-fluid">
    <div class="row">
        <div class="col12">
            <h1>Page</h1>
        </div>
    </div>
    <div class="row">
        <div class="col12">
            <table class="table table-dark table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ page.id }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ page.Auteur }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ page.Nom }}</td>
            </tr>
            <tr>
                <th>PublicationAt</th>
                <td>{{ page.PublicationAt ? page.PublicationAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ page.Description }}</td>
            </tr>
        </tbody>
    </table>
        </div>
    </div> 
</div>
    <a href="{{ path('app_page_index') }}" class="btn btn-primary btn-lg">Retour</a>

    <a href="{{ path('app_page_edit', {'id': page.id}) }}" class="btn btn-primary btn-lg">Modifier</a>

    {{ include('page/_delete_form.html.twig') }}
{% endblock %}
